
"use client"

import {
  Area,
  Bar,
  Cell,
  Label,
  Pie,
  Rectangle,
  Sector,
  XAxis,
  YAxis,
  ZAxis,
} from "recharts"

export {
  Area as AreaChart,
  Bar as BarChart,
  Cell as ChartCell,
  Label as ChartLabel,
  Pie as PieChart,
  Rectangle as ChartRectangle,
  Sector as ChartSector,
  XAxis as ChartXAxis,
  YAxis as ChartYAxis,
  ZAxis as ChartZAxis,
}

export * from "recharts"
