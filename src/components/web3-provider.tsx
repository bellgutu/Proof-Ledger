
'use client';
// This file is intentionally left blank. The new provider is at /src/components/wallet-provider.tsx
import React from 'react';

export default function Web3Provider({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
